## Agent skills

### Issue tracker

Issues 使用 `.scratch/<feature>/` 下的本地 Markdown 跟踪。见 `docs/agents/issue-tracker.md`。

### Triage labels

使用六个默认 ForgeFlow triage label。见 `docs/agents/triage-labels.md`。

### Domain docs

本仓库采用单一上下文布局。见 `docs/agents/domain.md`。
